# Nexus Autonomous Video Generator

High-performance hybrid video rendering pipeline leveraging Python for orchestration and C native binaries for long-duration rendering tasks (1–24 hours).

## System Requirements
- `gcc` or `clang` C compiler
- `ffmpeg` installed in system PATH

## Quick Start
```bash
python app.py
